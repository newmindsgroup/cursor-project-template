# Public Assets

Place static assets that should be served at the root URL here:

- `favicon.ico` - Site favicon
- `robots.txt` - Search engine directives
- `sitemap.xml` - Site map for SEO
- Any other files that need to be served from `/`

These files are copied to the build output directory as-is.
