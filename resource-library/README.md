# Resource Library (Local Only)

This folder is for local reference material that supports implementation.

- Add PDFs/MD locally as needed.
- Do not commit client-sensitive files.
- For shared references, use a separate private repo or approved shared storage.
